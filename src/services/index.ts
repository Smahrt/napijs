import userEndpoints from './user/user.endpoint';

/**
 * List of all endpoints. Add new endpoints here.
 * Enpdpoints not added here will not be available.
 */
const endpoints = [
  ...userEndpoints,
];

export default endpoints;
